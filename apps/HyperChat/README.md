# HyperChat placeholder

This workspace reserves `apps/HyperChat` for the future HyperChat application
import. HyperChat continues to be provided to LiveTL by the submodule at
`apps/LiveTL/src/submodules/chat` until that import happens.

The package scripts intentionally print TODO messages so root workspace
commands can include both applications during this intermediate phase.
