/* eslint-disable no-undef */
import '@testing-library/jest-dom/extend-expect';

// import { render, fireEvent } from '@testing-library/svelte';

// import { get, writable } from 'svelte/store';
// import Settings from '../../components/Settings.svelte';

describe('Settings menu', () => {
  it('shows the tabs', () => {
    // TODO Figure out how to get slider to render in jest
    // const { getByDisplayValue } = render(Settings, { isStandalone: false });
    // expect(getByDisplayValue('INTERFACE')).toBeInTheDocument();
    // expect(getByDisplayValue('FILTERS')).toBeInTheDocument();
  });
});